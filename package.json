{
  "name": "ollama-pr-reviewer",
  "version": "0.1.0",
  "private": true,
  "description": "PR review via a swappable LLM backend (Ollama locally, any OpenAI-compatible API in prod)",
  "type": "module",
  "scripts": {
    "review": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.67.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
